/**
 * We will pass the input lines here.
 * And start to write our solution from here.
 */
export class Solution {
  constructor(private inputs: number[]) {
    this.startSoln();
  }

  private startSoln() {
    console.log('Solution printed.', this.inputs);
  }
}
